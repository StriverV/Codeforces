#include<bits/stdc++.h>
#define ll long long
#define Naba ios_base::sync_with_stdio(false);cin.tie(0);cout.tie(0);
using namespace std;

int main()
{
    Naba;
    int TestCase;
    cin >> TestCase;
    while(TestCase--)
    {
        int n, k, x;
        cin >> n >> k >> x;

        vector<int>a(n);
        for(int i = 0; i < n; i++)
            cin >> a[i];

        vector<int>b(n);


    }

    return 0;
}